"""The web-search fabric tool: its request schema, provider backends, and egress.

A future fabric-served tool adds a sibling package under ``shared.tools`` with its own
schema, backends, and egress surface, reusing the generic ``shared.tools.contract`` and
``shared.tools.wire``.
"""
