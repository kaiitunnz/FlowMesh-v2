"""The web-search fabric tool: its request schema, provider backends, and egress.

Another fabric-served tool is a sibling package under ``shared.tools`` with its own
schema, backends, and egress surface, reusing the generic ``shared.tools.contract``.
"""
